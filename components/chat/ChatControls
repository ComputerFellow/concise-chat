import React from "react";
import { Switch } from "@/components/ui/switch";
import { Label } from "@/components/ui/label";
import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Zap, MessageSquare } from "lucide-react";

export default function ChatControls({ 
  model, 
  setModel, 
  conciseMode, 
  setConciseMode 
}) {
  return (
    <div className="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 p-4 bg-white/60 backdrop-blur-sm rounded-2xl border border-gray-200/50">
      <div className="flex items-center gap-4">
        <Tabs value={model} onValueChange={setModel} className="w-auto">
          <TabsList className="bg-gray-100">
            <TabsTrigger value="grok" className="gap-2">
              <Zap className="w-4 h-4" />
              Grok
            </TabsTrigger>
            <TabsTrigger value="kimi" className="gap-2">
              <MessageSquare className="w-4 h-4" />
              Kimi
            </TabsTrigger>
          </TabsList>
        </Tabs>
      </div>
      
      <div className="flex items-center gap-3">
        <Label 
          htmlFor="concise-mode" 
          className="text-sm font-medium text-gray-700 cursor-pointer"
        >
          Concise Mode
        </Label>
        <Switch
          id="concise-mode"
          checked={conciseMode}
          onCheckedChange={setConciseMode}
          className="data-[state=checked]:bg-indigo-600"
        />
      </div>
    </div>
  );
}